/**
 * loading 은 id 값을 prop 으로 받지 않는다.
 * 그냥 empty object임.
 */
export default function Loading(props) {
    // console.log(JSON.stringify(props))
    return (
        <h1>Loading a movie</h1>
    )
}